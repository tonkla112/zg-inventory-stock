{
  "name": "zg-inventory-stock",
  "private": true,
  "description": "Static browser app with a prebuilt React bundle for GitHub Pages.",
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "tailwindcss -c tailwind.config.js -i src/styles.css -o dist/styles.css --minify",
    "build:js": "node scripts/build-app.mjs",
    "test": "node tests/data.actions.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tailwindcss": "^3.4.17"
  }
}
